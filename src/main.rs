// retain
// String pop
// from_utf8
// from_utf8_lossy
// split_off

fn main() {
    let mut my_string = String::from("Hello there"); // 8, 9, 10 Vec<i32> .pop() -> i32
    println!("{:?}", my_string.pop());
    // Vec<u8> -> char
}
